# Artifex Studio — environment

# Base URL of the Artifex SDXL service (server-side only; the browser talks to
# same-origin /api/artifex/* which forwards here). Defaults to a local instance.
ARTIFEX_URL=http://tower.local:7860
